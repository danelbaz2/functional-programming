# תסריט לסרטון ההדגמה — 60 שניות

**פורמט:** YouTube, **Unlisted**. הקלטת מסך + הסבר קולי.
**כלי הקלטה:** OBS Studio (חינמי) או Xbox Game Bar (`Win+G`).
**רזולוציה:** 1920×1080. גופן טרמינל **גדול** — הבודק צופה בנייד לפעמים.

---

## הכנה לפני ההקלטה

- [ ] `sbt clean` ואז **הרצה אחת מלאה** כדי שכל התלויות כבר בקאש — אחרת ההורדה תיקח דקות
- [ ] `rm -rf data output` — כדי שהריצה בסרטון תיצור הכול מאפס
- [ ] פתח שני חלונות מסודרים: IntelliJ עם `TransactionJobs.scala` פתוח, וטרמינל
- [ ] הגדל גופן טרמינל ל-16-18pt
- [ ] סגור התראות, Slack, מייל
- [ ] הרץ חזרה יבשה אחת — למד כמה זמן `sbt run` באמת לוקח אצלך
- [ ] אם `sbt run` לוקח יותר מ-25 שניות: הקלט אותו בנפרד וקצר בעריכה, או הרץ ברקע והתחל את הצילום כשהפלט מתחיל לזרום

---

## התסריט — שנייה אחר שנייה

### 0:00-0:08 — פתיחה (מסך: IntelliJ, עץ הפרויקט פתוח)

> "שלום, אני << שם >>. הפרויקט הוא צינור עיבוד נתונים אנליטי ב-Scala על Apache Spark. הוא מנתח חמישה עשר אלף טרנזקציות מסחר אלקטרוני."

**פעולה:** גלול לאט בעץ הפרויקט כך שרואים `core`, `spark`, `io`, `model`.

---

### 0:08-0:18 — הארכיטקטורה (מסך: העץ, ואז `PureAnalytics.scala`)

> "הארכיטקטורה היא ליבה פונקציונלית טהורה עטופה במעטפת אימפרטיבית. חבילת core לא נוגעת בדיסק בכלל — אפס קלט פלט, אפס משתנים משתנים. חבילות io ו-spark הן היחידות שמבצעות אפקטים."

**פעולה:** פתח את `PureAnalytics.scala`, הצבע על ההערה בראש הקובץ. גלול ל-`classifyOrder` — ה-pattern matching נראה יפה במסך.

---

### 0:18-0:38 — הריצה (מסך: טרמינל)

**פעולה:** הקלד `sbt run` ולחץ Enter.

> "אני מריץ את הפייפליין. הוא מייצר את הדאטהסט מ-seed קבוע, קורא אותו חזרה, מבצע join בין הטרנזקציות לקטלוג המוצרים, ומחשב שישה דוחות."

**פעולה:** תן לפלט לזרום. כשמופיעים המקטעים — הצבע עליהם.

> "כאן רואים את הכנסות לפי קטגוריה, לפי מדינה, הלקוחות המובילים, והתפלגות גודל ההזמנות."

---

### 0:38-0:50 — נקודת השיא: נתונים פגומים ⭐

**פעולה:** הצבע על מקטע `SOURCE FILE QUALITY` בפלט.

> "וזו הנקודה החשובה. הדאטהסט מכיל בכוונה עשרים שורות פגומות — חמש עשרה עם מחיר לא מספרי, וחמש עם שדה חסר. התוכנית לא זורקת אף חריגה. כל שורה פגומה מוחזרת כערך Either עם תיאור השגיאה, ונכתבת לדוח נפרד."

**פעולה:** פתח את `output/rejected_lines/part-*.csv` — הראה את השורות עם ה-`reason`.

> "ארבעה עשר אלף תשע מאות ושמונים שורות תקינות, עשרים נדחות, אפס קריסות."

---

### 0:50-1:00 — סיום (מסך: `output/` ב-Explorer)

**פעולה:** פתח את תיקיית `output/` — הראה את שש תיקיות הדוחות.

> "שישה דוחות נכתבו לדיסק. כל הלוגיקה האנליטית נבדקת ב-ScalaTest בלי להפעיל Spark בכלל — כי היא טהורה. תודה."

---

## נקודות שחייבות להיראות בסרטון

הבודק מחפש את אלה. אם משהו לא נראה — הסרטון לא עשה את עבודתו.

- [ ] מבנה החבילות `core` / `spark` / `io` / `model`
- [ ] ריצה מוצלחת מקצה לקצה, ללא exception
- [ ] פלט הקונסולה עם הדוחות
- [ ] **קובץ `rejected_lines` עם שורות פגומות שטופלו** — זה הדגש המפורש של המרצה
- [ ] קבצי הפלט על הדיסק
- [ ] הסבר קולי לאורך כל הסרטון (לא מוזיקה, לא שקט)

---

## טעויות נפוצות שיעלו לך נקודות

| טעות | תיקון |
|---|---|
| הסרטון Private במקום Unlisted | הבודק לא יוכל לפתוח → אפס על הסעיף |
| גופן טרמינל קטן | לא קריא בנייד |
| הורדת תלויות sbt בתוך הסרטון | 40 שניות מתוך 60 מבוזבזות — הרץ פעם אחת לפני |
| אין קול | הדרישה היא "בליווי הסבר קולי" |
| ארוך מדי | ~60 שניות. עד 75 סביר, 120 כבר לא |
| מדבר על הקוד בלי להראות אותו | הצבע בעכבר על מה שאתה מתאר |

---

## אחרי ההקלטה

- [ ] העלאה ל-YouTube
- [ ] Visibility = **Unlisted** (לא Private, לא Public)
- [ ] העתקת הלינק ל-PDF
- [ ] בדיקה בגלישה פרטית שהלינק נפתח
